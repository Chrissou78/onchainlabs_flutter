name: onchainlabs_flutter_example
description: Example app for onchainlabs_flutter package.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

  # During local development, use a path dependency to your package root.
  onchainlabs_flutter:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
